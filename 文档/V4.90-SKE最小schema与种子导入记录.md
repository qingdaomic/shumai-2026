# V4.90 SKE 最小 schema 与种子导入记录

日期：2026-05-09

## 目标

执行 V4.89 确认的前置项：

- 创建 `prompt_skills`
- 创建 `prompt_skill_events`
- 导入 41 条 prompt Skill 种子数据
- 验证表、索引、记录数与幂等性

本阶段只操作线上 PostgreSQL 的 SKE 最小表与种子数据，不部署，不切换 PM2，不 reload Nginx，不修改线上代码目录。

## 模型建议

本阶段涉及服务器连接与数据库 schema 变更，建议使用 GPT-5.5 high。

原因：

- 需要先备份再执行 SQL
- 需要严格控制 additive migration 边界
- 需要验证幂等性与线上服务健康
- 不能误触部署、PM2、Nginx 或线上代码目录

## 执行前状态

服务器：

- 腾讯云香港服务器
- 用户：`ubuntu`
- 数据库：`shumai`
- 线上后端：`127.0.0.1:3001`

执行前数据库状态：

- `admin_operation_logs`：存在
- `users`：存在
- `prompt_skills`：不存在
- `prompt_skill_events`：不存在
- public 表数量：12

执行前健康检查：

- `http://127.0.0.1:3001/api/health` 正常
- 返回服务名：`树脉后端`
- version：`1.0.0`

## 备份

备份目录：

- `/home/ubuntu/shumai-backups/v4.90-ske-minimal-migration-20260509-182443`

备份文件：

- `postgres-pre-v4.90.dump`

备份大小：

- `176K`

备份 SHA256：

- `930894e2ec8167f903ed559822748617bb2992eda8efde0bec3e428070197487`

说明：

- 由于 postgres 用户不能穿透 `/home/ubuntu` 父目录，本阶段先将 dump 写入 `/tmp`，校验后移入备份目录。
- 这是备份路径权限处理，不影响数据库内容。

## 操作材料

schema migration：

- 来源：`server/migrations/20260508_ske_minimal.sql`
- 远端留档：`/home/ubuntu/shumai-backups/v4.90-ske-minimal-migration-20260509-182443/20260508_ske_minimal.sql`
- SHA256：`27480cc472d57571116f5a1fa072877ecbf3ae0fceeaa95efd9e26b2dbca90a7`

seed import SQL：

- 来源：`server/seeds/prompt_skills_seed.json`
- 远端留档：`/home/ubuntu/shumai-backups/v4.90-ske-minimal-migration-20260509-182443/v490_prompt_skills_seed_import.sql`
- SHA256：`c5a6c7a160fd511be3885b29ecd077017db7358c351a2bd9138bb2b54642fd06`

seed import SQL 特性：

- 由本地 `prompt_skills_seed.json` 生成
- 共 41 条
- `ON CONFLICT (skill_key) DO UPDATE`
- 可重复执行
- 不改 `server/schema.sql`

## 执行结果

首次执行结果：

- `prompt_skills`：创建成功
- `prompt_skill_events`：创建成功
- 7 个索引：创建成功
- seed 导入：`INSERT 0 41`

执行后验证：

- 表：`admin_operation_logs,prompt_skill_events,prompt_skills,users`
- public 表数量：14
- `prompt_skills` 列数：24
- `prompt_skill_events` 列数：12
- SKE 索引数：7
- `prompt_skills` 记录数：41
- 唯一 `skill_key`：41
- active Skill：41
- `prompt_skill_events` 记录数：0

Skill 类型分布：

- `calculate_fix`：1
- `diagnose`：4
- `final_layer`：1
- `hint`：3
- `lift`：1
- `method_hint`：15
- `quality_check`：1
- `read_question`：4
- `simplify`：1
- `socratic`：1
- `topic_patch`：4
- `transfer`：3
- `variant`：1
- `wrong_review`：1

## 幂等性验证

复跑 schema migration：

- `CREATE TABLE IF NOT EXISTS` 正常跳过已存在表
- `CREATE INDEX IF NOT EXISTS` 正常跳过已存在索引

复跑 seed import：

- `INSERT 0 41`
- `ON CONFLICT (skill_key) DO UPDATE` 正常

复跑后最终验证：

- public 表数量：14
- SKE 索引：
  - `idx_prompt_skill_events_question`
  - `idx_prompt_skill_events_skill`
  - `idx_prompt_skill_events_type`
  - `idx_prompt_skill_events_user`
  - `idx_prompt_skills_lookup`
  - `idx_prompt_skills_method`
  - `idx_prompt_skills_type`
- `prompt_skills` 约束数：16
- `prompt_skill_events` 约束数：6
- `prompt_skills` 记录数：41
- 唯一 `skill_key`：41
- active Skill：41
- `prompt_skill_events` 记录数：0

## 线上服务状态

执行后 PM2：

- root PM2：`shumai-api:online`
- 未新增 3002 进程
- 未切换 PM2
- 未保存 PM2

执行后健康检查：

- `http://127.0.0.1:3001/api/health` 正常
- 返回服务名：`树脉后端`
- version：`1.0.0`

## 本阶段边界

已执行：

- PostgreSQL 备份
- `prompt_skills` / `prompt_skill_events` additive migration
- 41 条 prompt Skill seed 导入
- schema 与 seed 幂等性验证
- 3001 后端健康检查

未执行：

- 未部署前端
- 未部署后端
- 未切换 release 目录
- 未启动 3002 旁路后端
- 未重启 PM2
- 未保存 PM2
- 未 reload Nginx
- 未修改 `/opt/shumai`
- 未修改 `/var/www/shumai`
- 未修改 `server/schema.sql`
- 未执行 study-plan / TTS / ASR / SVG / 微信 / 动画相关 SQL

## 结论

V4.90 已补齐后台 Skill 管理上线前的最小数据库基础。

现在数据库具备：

- `admin_operation_logs`
- `prompt_skills`
- `prompt_skill_events`
- 41 条 active prompt Skill

这意味着下一阶段可以进入 3002 release 后端旁路验证，重点验证：

- `GET /api/admin/skills`
- Skill 降权
- Skill 人工改写
- `prompt_skill_events` 是否按预期记录
- `admin_operation_logs` 是否记录 Skill 操作

## 下一步

建议下一阶段：

- **V4.91 后台 Skill 管理 3002 旁路验证**：基于干净 release 后端启动 3002，验证后台 Skill 管理 API、降权 / 改写和操作日志写读。
- **V4.92 后台操作日志正式上线验证**：在 V4.91 通过后，再考虑正式切换 release 后端与前端验证。
