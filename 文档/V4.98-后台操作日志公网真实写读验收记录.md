# V4.98 后台操作日志公网真实写读验收记录

日期：2026-05-09

## 目标

验证 V4.94 正式上线后的后台操作日志链路是否能在公网环境真实写入、读回和清理。

本阶段验证范围：

- 公网 `POST /api/admin/operation-logs`
- 公网 `GET /api/admin/operation-logs`
- 数据库 `admin_operation_logs` 按本阶段 `requestId` 清理
- 公网 `/api/health`
- PM2 状态只读确认

## 模型建议

本阶段涉及公网后台、鉴权、生产数据库读写与清理，建议使用 GPT-5.5 high。

## 执行方式

旧的 V4.96 短期管理员 token 已失效，公网接口返回 401。这是正常安全结果。

随后在服务器侧生成新的短期管理员 token：

- token 只保存在本机 `/tmp` 临时文件
- 未写入仓库
- 未写入文档
- 未输出 token
- 验收结束后已删除临时 token 文件

本阶段测试标识：

- `requestId`：`v4.98-prod-admin-log-20260509-200025`
- `targetKey`：`v4_98_prod_probe`
- `action`：`skill_manual_note`
- `source`：`v4.98-production-validation`

## 公网 API 写读结果

写入前公网读取：

- `GET /api/admin/operation-logs?limit=5`
- 返回：`ok:true`
- 返回：`stored:true`
- 当前条数：`0`

公网写入：

- `POST /api/admin/operation-logs`
- 返回：`ok:true`
- 返回：`stored:true`
- 测试日志 id：`2`
- `requestId` 命中：`v4.98-prod-admin-log-20260509-200025`
- `targetKey` 命中：`v4_98_prod_probe`
- `action` 命中：`skill_manual_note`

公网读回：

- `GET /api/admin/operation-logs?targetType=prompt_skill&targetKey=v4_98_prod_probe&limit=10`
- 返回：`ok:true`
- 返回：`stored:true`
- 返回条数：`1`
- 按 `requestId` 找到本阶段测试日志：是
- 命中 id：`2`

## 清理结果

清理方式：

- 读取服务器现有 `.env` 中的数据库连接配置
- 不输出连接串
- 只按本阶段唯一 `requestId` 删除测试日志

清理结果：

- 清理前 `admin_operation_logs`：`1`
- 按 `requestId` 匹配：`1`
- 删除：`1`
- 删除 id：`2`
- 清理后 `admin_operation_logs`：`0`

清理脚本包装期间出现过两次命令包装错误：

- 一次是 shell `source .env` 不能处理带空格的说明字段
- 一次是交互式 Node stdin 被 `sudo -S` 吞掉了前半段脚本

这两次没有完成删除操作，也没有扩大 SQL 影响范围。最终采用 `psql` 按 `requestId` 精确删除。

## 最终复验

公网操作日志：

- `GET /api/admin/operation-logs?limit=10`
- 返回：`ok:true`
- 返回：`stored:true`
- 当前条数：`0`

公网 health：

- `GET /api/health`
- 返回：`status:ok`
- 返回：`name:树脉后端`
- 返回：`version:1.0.0`

PM2 只读确认：

- 当前进程：`shumai-api-v4.94`
- 状态：`online`
- cwd：`/opt/shumai-releases/v4.94-20260509-190052/repo/server`

## 本阶段边界

已执行：

- 服务器连接
- 生成短期管理员 token
- 公网后台操作日志写入
- 公网后台操作日志读回
- 按本阶段 `requestId` 删除测试日志
- 公网 health 复验
- PM2 状态只读查看

未执行：

- 未部署
- 未修改 Nginx
- 未 reload Nginx
- 未重启 PM2
- 未保存 PM2
- 未切换后端 release
- 未执行 migration
- 未修改数据库 schema
- 未修改线上代码
- 未触碰 study-plan / TTS / ASR / SVG / 微信 / 动画相关线上能力

## 结论

V4.98 公网后台操作日志真实写读验收通过。

生产环境的 `admin_operation_logs` 可以通过公网后台 API 完成持久化写入和条件读回；本阶段测试数据已清理，最终表记录数回到 `0`。

## 下一步

建议下一阶段：

- **V4.99 部署脚本专项校准**：审查 `deploy/` 与根目录部署文档中的旧路径和服务器实际 root 是否一致。
- 如果只做文档/脚本审查，模型建议 GPT-5.4-Mini。
- 如果要连接服务器复核 Nginx、执行 dry-run 或修改线上脚本，模型建议 GPT-5.5 high。
