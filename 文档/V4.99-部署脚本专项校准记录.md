# V4.99 部署脚本专项校准记录

日期：2026-05-09

## 目标

审查并校准 `deploy/` 与根目录部署文档中的旧路径、旧进程名和旧 PM2 假设，使其与当前线上事实一致。

本阶段只做本地审查和文档/脚本调整，不连接服务器，不部署，不重启 PM2，不 reload Nginx，不执行 SQL。

## 模型建议

本阶段只涉及本地文档与脚本校准，建议使用 GPT-5.4-Mini。

若后续需要连接服务器复核 Nginx、执行 release dry-run 或调整线上脚本，则切换到 GPT-5.5 high。

## 执行前状态

- `git status -sb` 显示工作区已有大量历史脏改
- `git log --oneline -8` 最新提交为 `edf5357 docs: record admin operation log production validation`
- 线上事实已由 V4.96-V4.98 固化：
  - 前端 root：`/opt/shumai/dist`
  - 后端进程：`shumai-api-v4.94`
  - PM2：root 运行
  - 发布路线：`/opt/shumai-releases/`

## 执行过程

- 审查了 `DEPLOY.md`
- 审查了 `deploy/deploy.sh`
- 审查了 `deploy/deploy-backend.sh`
- 审查了 `deploy/nginx-shumai.conf`
- 对比了 V4.96、V4.97、V4.98 的验收与校准记录
- 将明显不符合当前线上事实的路径与进程名改成当前标准

## 验证结果

- 文档与脚本中的前端根目录已统一向 `/opt/shumai/dist` 收敛
- 后端部署说明已从直接线上重启转为 release 目录语义
- Nginx 配置样例已对齐当前静态 root
- 未执行服务器级验证

## 修改文件

- `DEPLOY.md`
- `deploy/deploy.sh`
- `deploy/deploy-backend.sh`
- `deploy/nginx-shumai.conf`
- `tasks.md`
- `文档/树脉系统建设记录.md`

## 边界

- 未部署
- 未连接服务器
- 未 reload Nginx
- 未重启 PM2
- 未保存 PM2
- 未执行 SQL
- 未修改数据库 schema
- 未修改线上文件
- 未触碰 `server/schema.sql`
- 未触碰 `study-plan`、TTS、ASR、SVG、微信、动画相关线上能力

## 结论

V4.99 本地部署脚本专项校准完成，部署文档与脚本已开始统一到当前线上事实。

## 下一步

- 复核本阶段 diff，确认没有引入不必要的部署语义变化
- 如需继续推进到线上，只做 release dry-run 或只读核验
- 后续模型建议：本地审查 GPT-5.4-Mini，服务器相关 GPT-5.5 high
