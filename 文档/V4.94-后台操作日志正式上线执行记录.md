# V4.94 后台操作日志正式上线执行记录

日期：2026-05-09

## 目标

按 V4.92 操作卡执行正式上线：

- 新建干净 release
- 构建前端
- 准备后端
- 备份当前前端与 PM2 状态
- 3002 最终预检
- 切换正式 3001 后端
- 发布前端 dist
- 验证公网与后台 API

本阶段是正式上线执行阶段。

## 模型建议

本阶段涉及正式 3001 后端切换、前端发布和回滚路径，建议使用 GPT-5.5 high。

## release 与快照

release 目录：

- `/opt/shumai-releases/v4.94-20260509-190052/repo`

release HEAD：

- `95d155b2f452707fc68da6a1329c242cabe94f28`

快照目录：

- `/home/ubuntu/shumai-backups/v4.94-cutover-20260509-190052`

快照内容：

- `/var/www/shumai` 切换前静态文件
- PM2 切换前列表
- 3001 切换前 health
- 3001 切换后 health
- 公网 health
- 公网页面 HTML 抓取结果

## 构建与预检

release 状态：

- `git status --short`：0

前端：

- `npm ci`：通过
- `npm run build`：通过
- `dist` 大小：`1.5M`

后端：

- `npm ci --omit=dev`：通过
- `node --check index.js`：通过

切换前 3001：

- `{"status":"ok","name":"树脉后端","version":"1.0.0"}`

切换前 PM2：

- `shumai-api:online`

## 3002 最终预检

临时进程：

- `shumai-api-v4.94-precheck`
- 端口：3002
- `--no-autorestart`
- 不保存 PM2

3002 health：

- `{"status":"ok","name":"树脉后端","version":"1.0.0"}`

3002 API：

- `GET /api/admin/skills?limit=20`：通过
  - items：20
  - summary total：41
- `GET /api/admin/operation-logs?limit=10`：通过
  - items：0
- `POST /api/skills/recommend`：通过
  - source：`db`
  - returned：2

预检后：

- `shumai-api-v4.94-precheck` 已 stop/delete
- PM2 回到 `shumai-api:online`

## 后端正式切换

执行策略：

- 停止旧 `shumai-api`
- 启动新 `shumai-api-v4.94`
- 新进程监听 3001
- 暂不 `pm2 save`
- 旧 `shumai-api` 保留为 stopped，便于回滚

切换后 PM2：

- `shumai-api:stopped`
- `shumai-api-v4.94:online`

切换后 health：

- 本机 3001：`{"status":"ok","name":"树脉后端","version":"1.0.0"}`
- 公网：`{"status":"ok","name":"树脉后端","version":"1.0.0"}`

3001 API：

- `GET /api/admin/skills?limit=20`：通过
  - items：20
  - summary total：41
- `GET /api/admin/operation-logs?limit=10`：通过
  - items：0
- `POST /api/skills/recommend`：通过
  - source：`db`
  - returned：2

数据库计数：

- `prompt_skills`：41
- `prompt_skill_events`：0
- `admin_operation_logs`：0

## 前端发布

发布方式：

- `rsync -a --delete "$RELEASE/repo/dist/" /var/www/shumai/`

发布结果：

- `dist` 顶层文件数：5
- `/var/www/shumai` 发布前顶层文件数：12
- `/var/www/shumai` 发布后顶层文件数：5
- `index.html` 大小：2950 bytes
- `manifest.json`：存在

公网验证：

- `https://shumai.cc/`：HTTP 200，2622 bytes
- `https://shumai.cc/?view=admin`：HTTP 200，2622 bytes
- `https://shumai.cc/manifest.json`：HTTP 200，632 bytes
- `index.html` 包含 `id="root"`
- assets：
  - `/assets/index-BqO7Cdc5.js`
  - `/assets/vendor-libs-DYLXRpC5.js`
  - `/assets/vendor-react-Bpq61jYu.js`
  - `/assets/data-D-iFhHDm.js`

公网 API：

- `https://shumai.cc/api/health`：正常，name 为 `树脉后端`

## 观察

完成 3 轮观察。

第 1 轮：

- 时间：`2026-05-09_19:02:43`
- public health：ok
- local health：ok
- PM2：`shumai-api:stopped,shumai-api-v4.94:online`
- DB：`prompt_skills=41,prompt_skill_events=0,admin_operation_logs=0`

第 2 轮：

- 时间：`2026-05-09_19:03:44`
- public health：ok
- local health：ok
- PM2：`shumai-api:stopped,shumai-api-v4.94:online`
- DB：`prompt_skills=41,prompt_skill_events=0,admin_operation_logs=0`

第 3 轮：

- 时间：`2026-05-09_19:04:44`
- public health：ok
- local health：ok
- PM2：`shumai-api:stopped,shumai-api-v4.94:online`
- DB：`prompt_skills=41,prompt_skill_events=0,admin_operation_logs=0`

## 当前线上状态

后端：

- 正式 3001 当前由 `shumai-api-v4.94` 承载
- 旧 `shumai-api` 处于 stopped 状态
- 未执行 `pm2 save`

前端：

- `/var/www/shumai` 已发布 V4.94 release 的 `dist`

数据库：

- `admin_operation_logs` 可用
- `prompt_skills` 可用
- `prompt_skill_events` 可用
- 41 条 prompt Skill 保持不变

Nginx：

- 未 reload
- 未修改配置

## 回滚路径

后端回滚：

```bash
sudo pm2 stop shumai-api-v4.94
sudo pm2 delete shumai-api-v4.94
sudo pm2 restart shumai-api
curl -fsS http://127.0.0.1:3001/api/health
curl -fsS https://shumai.cc/api/health
```

前端回滚：

```bash
sudo rsync -a --delete /home/ubuntu/shumai-backups/v4.94-cutover-20260509-190052/var-www-shumai-before/ /var/www/shumai/
```

数据库：

- 不做破坏性回滚
- 如有异常，先回滚应用后端与前端，再单独评估数据库

## 本阶段边界

已执行：

- 新建 V4.94 release
- 前端构建
- 后端生产依赖安装
- 后端语法检查
- `/var/www/shumai` 快照
- PM2 切换前快照
- 3002 最终预检
- 正式 3001 后端切换
- 前端 dist 发布
- 公网首页 / admin / manifest / health 验证
- 3 轮观察

未执行：

- 未修改 `/opt/shumai`
- 未 reload Nginx
- 未保存 PM2
- 未删除旧 `shumai-api`
- 未执行 SQL
- 未修改数据库 schema
- 未触碰 study-plan / TTS / ASR / SVG / 微信 / 动画相关线上能力

## 结论

V4.94 正式上线成功。

当前线上后端已从旧 `/opt/shumai` 进程切换到干净 release 的 `shumai-api-v4.94`，前端也已发布 release `dist`。

后台 Skill 管理、后台操作日志读取、SKE 数据库推荐和公网 health 均验证通过。

## 下一步

建议下一阶段：

- **V4.95 上线后稳定化与 PM2 持久化决策**：继续观察线上状态，确认是否删除旧 stopped `shumai-api`、是否将新进程转为长期命名、是否执行 `pm2 save`。
