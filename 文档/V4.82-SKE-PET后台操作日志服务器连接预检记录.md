# V4.82 SKE/PET 后台操作日志服务器连接预检记录

日期：2026-05-09

## 目标

在用户明确授权后，开始执行后台操作日志真实上线验证的服务器连接预检。

本阶段原计划：

- 登录香港服务器
- 确认 `/opt/shumai` 的 Git 状态
- 确认服务器代码是否可拉取到最新 `main`
- 在执行任何数据库 migration 前先做环境确认

## 已执行的本地检查

- 本地 `main` 已同步到 `origin/main`
- 远端最新 commit 为 `d7c08fb docs: plan admin operation log rollout`
- V4.81 上线验证前检查清单已经提交并推送
- 本地仍存在历史遗留脏改与未跟踪文件，未清理、未 reset、未 revert

## 服务器连接结果

已完成服务器连接预检：

- 主机可达
- SSH 服务可响应
- 公钥免密不可用，已改用用户授权后的密码登录
- 使用修正后的密码成功登录 `ubuntu`
- 当前登录用户为 `ubuntu`
- 服务器主机为 `VM-0-17-ubuntu`

## 服务器 Git 状态

服务器代码目录：

- `/opt/shumai`

预检结果：

- `/opt/shumai` 属主为 `root:root`
- `/opt/shumai/.git` 属主为 `root:root`
- `ubuntu` 用户不能直接执行 `git fetch` / `git pull`
- 使用 `sudo git fetch origin main` 仅更新了远端引用，没有改工作区
- 服务器当前 `HEAD`：`833e245d0bd9b361ef20d996befe09298de53ff8`
- 服务器 `origin/main` 更新后：`d7c08fba958f7cdd5677a2c242aad5dd0e00be0c`
- 服务器相对 GitHub 最新 `main` 落后 50 个提交
- 服务器工作区有 43 个 tracked 脏改
- 服务器工作区有 23 个未跟踪项

与本次后台操作日志上线强相关的文件状态：

- `server/api/admin.js`：服务器已有脏改，同时 GitHub 最新也修改了该文件
- `src/App.jsx`：服务器已有脏改，同时 GitHub 最新也修改了该文件
- `tasks.md`：服务器已有脏改，同时 GitHub 最新也修改了该文件
- `server/migrations/20260509_admin_operation_logs.sql`：GitHub 最新新增，服务器当前工作区不存在
- `server/services/admin-operation-logs.js`：GitHub 最新新增，服务器当前工作区不存在

因此，当前不能安全执行 `git pull` 或部署。直接拉取会面临覆盖线上脏改、合并冲突或破坏当前线上服务的风险。

## 服务状态只读检查

- `http://localhost:3001/api/health` 正常返回
- `https://shumai.cc/api/health` 正常返回
- 3001 端口有 Node 进程监听
- 监听进程为 `root` 用户运行的 `node /opt/shumai/server/index.js`
- `root` 用户下存在 PM2 daemon
- `ubuntu` 用户下的 `pm2 list` 为空

这说明当前线上后端不是由 `ubuntu` 用户的 PM2 列表管理，而是由 `root` 用户 PM2 或 root 进程链路管理。

## 本阶段未执行

- 未执行 `git pull`
- 未合并 GitHub 最新代码
- 未修改 `/opt/shumai` 工作区文件
- 未修改 `/var/www/shumai`
- 未读取服务器环境变量
- 未备份 PostgreSQL
- 未执行 migration SQL
- 未重启 PM2
- 未修改 Nginx
- 未部署前端
- 未部署后端
- 未访问或改动线上数据库

## 安全边界

由于服务器代码目录 root-owned、工作区存在大量脏改，并且本次所需文件与脏改重叠，本阶段立即停止上线验证，没有继续执行 migration、部署或 PM2 重启。

本阶段唯一使用 sudo 的操作：

- `sudo git fetch origin main`

该操作只更新 `.git` 内的远端引用，不改工作区，不部署，不影响正在运行的服务。

本阶段没有触碰：

- `server/schema.sql`
- `deploy/`
- TTS / ASR
- study-plan / daily / admin / svg / wechat / parent / bot
- `public/animations/`
- `dist-singlefile/`
- `local-notes/`
- `.env*`

## 下一步

继续真实上线验证前，需要先解决服务器代码目录治理问题：

- 先备份 `/opt/shumai` 当前目录和关键线上配置
- 生成服务器当前脏改清单，区分历史遗留、线上必要改动和可废弃文件
- 明确是否允许把 `/opt/shumai` 迁移为干净 Git 工作区
- 明确 PM2 管理用户：当前实际后端由 root 进程链路运行
- 再决定是修复原目录，还是新建干净 release 目录后切换服务路径

代码目录治理完成后，可进入下一阶段：

- **V4.83 服务器代码目录治理与上线前备份**：备份 `/opt/shumai`、导出脏改清单、确认 root/ubuntu 管理边界。
- **V4.84 后台操作日志真实上线验证**：服务器版本确认、数据库备份、执行 `server/migrations/20260509_admin_operation_logs.sql`、后端重启、API 与前端验证。
