# V4.83 服务器代码目录上线前备份与清单导出记录

日期：2026-05-09

## 目标

在不部署、不拉代码、不执行 SQL 的前提下，先保护服务器当前现场，并导出足够清晰的上线前判断依据。

V4.82 已经确认后台操作日志不能直接上线：服务器 `/opt/shumai` 落后 GitHub 最新 `main` 50 个提交，同时存在大量线上脏改。本阶段只做备份与清单导出，避免把线上历史状态和后台操作日志 migration 混在一起。

## 服务器备份结果

备份目录：

- `/home/ubuntu/shumai-backups/v4.83-20260509-173223`

备份归档：

- `opt-shumai-and-var-www-shumai.tar.gz`

备份范围：

- `/opt/shumai`
- `/var/www/shumai`

备份结果：

- tar 状态：`0`
- 归档大小：`36M`
- 归档 SHA256：`a7081a8ef4c1e1daae989098293cb3613f298dd841eeaa3f9a06a14bb97f0e6d`
- 备份目录内记录文件数：`23`

## 导出的清单文件

备份目录内已保存：

- `summary.txt`
- `git-status-sb.txt`
- `git-status-porcelain.txt`
- `git-log-head.txt`
- `git-log-origin-main.txt`
- `git-diff-stat.txt`
- `git-diff-name-only.txt`
- `git-untracked-files.txt`
- `git-behind-name-only.txt`
- `key-dirty-files.patch`
- `v4.83-relevant-dirty-files.txt`
- `v4.83-relevant-behind-files.txt`
- `permissions.txt`
- `disk-and-size.txt`
- `processes-node-pm2.txt`
- `port-3001.txt`
- `pm2-root-list.txt`
- `pm2-ubuntu-list.txt`
- `health-localhost-3001.json`
- `health-public.json`
- `backup-sha256.txt`
- `result.txt`

## 关键诊断结果

服务器版本：

- 当前 `HEAD`：`833e245d0bd9b361ef20d996befe09298de53ff8`
- `origin/main`：`d7c08fba958f7cdd5677a2c242aad5dd0e00be0c`
- ahead / behind：`0 / 50`
- GitHub 最新相对服务器新增或修改文件数：`142`

服务器工作区：

- tracked 脏改：`43`
- 未跟踪项：`23`
- 当前脏改统计：`43 files changed, 1884 insertions(+), 230 deletions(-)`

与后台操作日志上线强相关的服务器脏改：

- `server/api/admin.js`
- `server/index.js`
- `server/schema.sql`
- `src/App.jsx`
- `tasks.md`

与后台操作日志上线强相关的 GitHub 最新变更：

- `server/api/admin.js`
- `server/migrations/20260509_admin_operation_logs.sql`
- `server/services/admin-operation-logs.js`
- `src/App.jsx`
- `tasks.md`

## 权限与进程状态

目录权限：

- `/opt/shumai`：`root:root`
- `/opt/shumai/.git`：`root:root`
- `/opt/shumai/server`：`root:root`
- `/var/www/shumai`：`www-data:www-data`

后端进程：

- `root` 用户 PM2 中存在 `shumai-api`
- `shumai-api` 状态：`online`
- 进程：`node /opt/shumai/server/index.js`
- `ubuntu` 用户 PM2 列表为空
- 3001 端口有 Node 进程监听

健康检查：

- `http://localhost:3001/api/health` 正常
- `https://shumai.cc/api/health` 正常

## 本阶段执行过的服务器操作

- 读取磁盘空间与目录大小
- 创建 `/home/ubuntu/shumai-backups/v4.83-20260509-173223`
- 导出 Git 状态、diff 统计、未跟踪文件、落后文件清单
- 导出相关关键文件 patch
- 导出权限、PM2、进程、端口和健康检查记录
- 使用 `sudo tar` 备份 `/opt/shumai` 与 `/var/www/shumai`
- 使用 `sudo chown` 将备份目录归还给 `ubuntu`
- 生成 tar 包 SHA256

## 本阶段未执行

- 未执行 `git pull`
- 未执行 `git checkout`
- 未覆盖服务器代码
- 未修改 `/opt/shumai` 工作区文件
- 未修改 `/var/www/shumai`
- 未执行 SQL
- 未备份或修改 PostgreSQL
- 未部署前端
- 未部署后端
- 未重启 PM2
- 未修改 Nginx
- 未切换线上服务路径

## 结论

当前不适合在原 `/opt/shumai` 目录直接上线后台操作日志。主要原因：

- 线上目录由 `root` 管理，`ubuntu` 不能直接拉取或修复 Git
- 线上代码落后 GitHub 最新 50 个提交
- 线上工作区存在 43 个 tracked 脏改与 23 个未跟踪项
- 本次后台操作日志相关文件与线上脏改重叠
- 后端由 root PM2 管理，不能按 `ubuntu` 用户 PM2 流程重启

## 下一步

建议下一阶段改为：

- **V4.84 服务器干净 release 目录方案**：不覆盖 `/opt/shumai`，先设计或创建独立干净目录，例如 `/opt/shumai-release-v4.84`，从 GitHub 最新 `main` 拉取，独立安装依赖和构建验证。
- **V4.85 线上切换前演练**：确认环境变量、PM2 root 管理方式、Nginx 静态目录、数据库备份和回滚路径。
- **V4.86 后台操作日志真实上线验证**：在干净 release 和数据库备份就绪后，再执行 migration、后端重启和前端验证。
