# V4.24 提交拆分清单

> 阶段：树脉 V4.24｜.gitignore 补漏与提交拆分清单
> 日期：2026-05-08
> 窗口：2号窗口，本地与服务器共同执行任务
> 边界：本阶段只做本地整理；不 `git add`、不 `git commit`、不 `git push`、不部署、不重启 PM2、不删除文件。

## 1. 总体原则

- 不做一个巨型提交。
- V4 前端、V4 文档截图、后端、TTS/ASR、动画、部署脚本、SKE 必须分开。
- 不提交 `local-notes/`。
- 不提交 `.env` / `.env.*` / `*.local`。
- 不提交 `video/`。
- 不提交 `server/cosyvoice/`。
- 不提交真实密钥、云账号密码、token、SecretId / SecretKey。
- `dist-singlefile/` 已有 5 个跟踪文件，暂不处理，后续单独决定是否从 Git 跟踪中移除。
- `public/animations/` 暂保留观察，后续单独判断是线上资源还是可再生成产物。

## 2. 建议提交顺序

### Commit 1：V4 总入口与账户中心前端稳定版

目标：
提交已经线上验证通过的 V4 前端体验。

候选文件：

- `src/App.jsx`
- `index.html`
- `public/manifest.json`
- `public/sw.js`
- `src/data/*` 中确实与 V4 功能强相关的文件

包含内容：

- 总首页双学科入口
- 数学分支二级首页
- 英语规划页
- 微信 AI 独立页面
- 我的页绑定区
- 账户中心
- 主题切换
- 移动端适配
- 总入口通用导航边界

注意：

- 不混入 `server/`。
- 不混入 TTS / ASR。
- 不混入动画生成。
- 不混入部署脚本。

### Commit 2：V4 文档与截图记录

目标：
提交 V4.14-V4.22 的建设记录、设计规范、截图证据，以及 V4.23-V4.24 的整理记录。

候选文件：

- `tasks.md`
- `文档/树脉系统建设记录.md`
- `文档/树脉V4界面重建设计规范.md`
- `文档/重启后继续提示词.md`，如确实需要同步更新
- `文档/V4.23本地工作区整理报告.md`
- `文档/V4.24提交拆分清单.md`
- `文档/截图记录/`

包含内容：

- V4 阶段记录
- 截图入档规则
- 总入口壳层规则
- 账户中心策略
- 线上部署根目录发现：`/opt/shumai/dist`
- 本地工作区风险分组
- `.gitignore` 安全补漏结论

注意：

- 截图文件可以多，提交前再确认大小。
- 不提交 `local-notes/`。
- 不把云账号、密钥、服务器密码写入文档。

### Commit 3：部署文档与脚本修正，需单独审查

目标：
修正部署认知，避免继续误用 `/var/www/shumai`。

候选文件：

- `DEPLOY.md`
- `deploy/deploy.sh`
- `deploy/deploy-backend.sh`
- `deploy/nginx-shumai.conf`
- `server/ecosystem.config.cjs`

包含内容：

- 线上实际 Nginx root 是 `/opt/shumai/dist`。
- 部署脚本是否应按 `nginx -T` 实际配置调整。
- 前端静态同步流程。
- 后端部署与 PM2 重启边界。

注意：

- 先审查脚本内容。
- 不在 V4.24 阶段提交。
- 不修改服务器。
- 不和前端 UI 或后端功能混在一个提交里。

### Commit 4：后端稳定能力候选，需单独审查

目标：
把后端改动从前端提交中隔离出来。

候选文件：

- `server/index.js`
- `server/schema.sql`
- `server/api/*`
- `server/services/*`
- `server/bot.js`
- `server/prompts/*`
- `server/package.json`

注意：

- 当前不适合整体提交。
- 需要按功能拆：认证 / 微信 / 学习计划 / TTS / ASR / SKE。
- 涉及 schema 的变更必须单独说明迁移影响。
- 不能和 V4 前端提交混在一起。
- 不能把语音、动画定时任务和稳定后端能力混在一个提交里。

### Commit 5：TTS / ASR 语音能力，暂缓

目标：
暂时不进入主提交。

候选文件：

- `server/api/tts.js`
- `server/api/asr.js`
- `server/services/tts.js`
- `server/services/tencent-sign.js`
- `server/scripts/*`
- `server/cosyvoice/`

注意：

- `server/cosyvoice/` 已被 `.gitignore`。
- 需要单独检查依赖、密钥、成本、部署资源。
- 需要单独验证手机端语音录入和转文字。
- 不与前端 V4 或 SKE 混提交。

### Commit 6：动画 / 视频 / HyperFrames，暂缓

目标：
隔离大文件和生成式资源。

候选文件：

- `video/`
- `public/animations/`
- `server/services/animation-cron.js`
- `server/services/final-animation-generator.js`

注意：

- `video/` 已被 `.gitignore`，不提交。
- `public/animations/` 暂保留观察。
- 如果 `public/animations/` 是线上必须访问资源，需要单独制定资源发布策略。
- 如果 `public/animations/` 可再生成，应考虑忽略或外部存储。
- 动画定时任务上线前必须确认 2核2G 服务器压力和开关策略。

### Commit 7：SKE 教学 Skill 引擎，后续新阶段

目标：
作为新的后端 + 前端闭环任务，不混入 V4 前端提交。

候选文件：

- `server/schema.sql`
- `server/api/skills.js`
- `server/services/prompt-skills.js`
- `server/prompts/skill-seeds.js`
- `server/api/ai.js`
- `src/App.jsx`
- `PageDetail` / `CoachPanel` 相关代码

注意：

- SKE-1 先做数据库结构和种子 Skill。
- 后续再做推荐 API、AI Skill 调用、前台气泡、后台管理。
- 不与 TTS / ASR / 动画混提交。
- 不把 SKE 前置想法和 V4 前端稳定成果放在同一个提交里。

## 3. 不提交 / 暂缓清单

### 永远不提交

- `local-notes/`
- `.env`
- `.env.*`
- `*.local`
- 真实密钥
- 云账号密码
- token
- SecretId / SecretKey
- `node_modules/`

### 暂缓提交

- `video/`
- `server/cosyvoice/`
- `public/animations/`
- `dist-singlefile/`
- TTS / ASR 相关后端
- 动画定时任务相关后端

## 4. dist-singlefile 特别说明

当前 `dist-singlefile/` 已被 `.gitignore` 规则覆盖，但有 5 个文件已被 Git 跟踪：

- `dist-singlefile/icon-192.svg`
- `dist-singlefile/icon-512.svg`
- `dist-singlefile/index.html`
- `dist-singlefile/manifest.json`
- `dist-singlefile/sw.js`

本阶段不处理，不执行 `git rm --cached`。

后续可选方案：

- 如果 `dist-singlefile/` 已不再用于部署，可单独开任务移出 Git 跟踪。
- 如果仍用于某种单文件发布，应保留并明确用途。
- 不要在 V4 前端提交里混处理它。

## 5. 当前工作区提醒

当前工作区仍然包含多阶段混合改动：

- V4 前端 UI 和总入口壳层。
- V4 文档与截图。
- 后端 API、schema、微信、学习计划等变化。
- TTS / ASR 语音相关新文件。
- 动画服务和 `public/animations/`。
- 部署脚本和运维配置。
- 已被跟踪的 `dist-singlefile/` 文件。

所以后续真正提交时，必须先按上面的顺序拆分，不要用 `git add .`。

## 6. 建议下一步

优先级建议：

1. 先由总控窗口决定是否进入 GitHub 提交拆分执行。
2. 若要进入 SKE-1，先确保本地提交边界清楚。
3. SKE-1 只做数据库结构与种子 Skill，不碰 TTS / ASR / 动画。
4. 部署脚本修正单独开窗口，不和 SKE 混做。
