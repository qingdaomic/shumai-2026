# V4.29 后端低风险稳定提交记录

> 阶段：树脉 V4.29｜后端低风险稳定提交执行
> 日期：2026-05-08
> 窗口：2号窗口，本地与服务器共同执行任务
> 边界：本阶段只创建本地 Git commit；不 push、不部署、不服务器操作、不重启 PM2、不改数据库、不删除文件。

## 1. 本阶段目标

V4.29 只把 V4.28 审查通过的低风险后端成果提交到本地 Git 历史，同时把 V4.27-V4.29 的审查过程写成独立文档提交。

后端提交严格限制为：

- `server/api/ai.js`
- `server/prompts/tutor.js`
- `server/api/teacher.js`

文档提交严格限制为：

- `tasks.md`
- `文档/树脉系统建设记录.md`
- `文档/V4.27后端变更拆分报告.md`
- `文档/V4.28后端稳定能力审查报告.md`
- `文档/V4.29后端低风险稳定提交记录.md`

## 2. 后端文件复核

### `server/api/ai.js`

变更内容：

- 学习路径 system prompt 中的“数脉学长”统一为“树脉学长”。

复核结论：

- 不依赖 `server/index.js` 的新增挂载。
- 不依赖未部署 schema。
- 不依赖 TTS / ASR / 动画。
- 不包含真实密钥、token、账号密码。
- 属于品牌与提示词统一，不改变危险 API 行为。

### `server/prompts/tutor.js`

变更内容：

- AI 学伴提示词品牌统一为“树脉学长”。
- 语音讲解脚本提示词升级为名师式讲解，强调题眼、破局、误区和迁移口诀。

复核结论：

- 不依赖 `server/index.js` 的新增挂载。
- 不依赖未部署 schema。
- 不依赖 TTS / ASR / 动画服务上线。
- 不包含真实密钥、token、账号密码。
- 只改变 AI 输出风格，不引入新服务依赖。

### `server/api/teacher.js`

变更内容：

- 班级报告新增 `tomorrowAdvice`，包含讲评焦点、原因、10 分钟流程、推荐题、关注学生和分层名单。

复核结论：

- 使用既有 `classes`、`class_students`、`users`、`progress`、`wrong_questions`、`checkins` 表。
- 不依赖未部署 schema。
- 不依赖 TTS / ASR / 动画。
- 不包含真实密钥、token、账号密码。
- 属于 additive response，不删除旧字段，不破坏原响应结构。

## 3. 已运行检查

本阶段运行：

- `node --check server/api/ai.js`
- `node --check server/prompts/tutor.js`
- `node --check server/api/teacher.js`
- `git diff --check`

三份后端文件的 Node 语法检查均通过。敏感关键词复核未确认发现明文真实云密钥；看到的是已有 OpenAI SDK 引用和 token 数量参数等正常代码。

## 4. 明确未纳入本轮提交

本轮后端 commit 不纳入：

- `server/index.js`
- `server/schema.sql`
- `server/package.json`
- `server/api/wechat.js`
- `server/bot.js`
- `server/api/parent.js`
- `server/api/study-plan.js`
- `server/services/daily.js`
- `server/api/admin.js`
- `server/api/svg.js`
- TTS / ASR 相关文件
- 动画 / HyperFrames 相关文件
- `public/animations/`
- `deploy/`
- `dist-singlefile/`
- `local-notes/`
- `.env*`

原因：

- 上述文件仍然涉及 schema、语音、动画、部署、服务器资源统计或本地私密边界。
- V4.29 的价值是先沉淀低风险成果，而不是把后端混合栈一次性塞进 Git。

## 5. 本阶段提交边界

计划创建两个本地 commit：

1. `feat: refine low-risk AI tutoring backend modules`
   - 只包含低风险后端文件。
2. `docs: record backend review and low-risk commit boundary`
   - 只包含 V4.27-V4.29 审查与边界文档。

本阶段不执行 `git push`。是否推送由下一阶段单独确认。

## 6. 下一步建议

V4.29 完成后，有两个稳妥选择：

1. 先推送这两个干净 commit 到 GitHub。
2. 或继续做 V4.30：数据库 / schema 迁移拆分计划。

如果要尽快进入 SKE-1，建议先完成 schema 拆分，因为当前 `server/schema.sql` 已经混有学习计划、微信上下文、SVG、TTS 用量等多类变更。
