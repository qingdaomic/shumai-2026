# V5.17 V5.16 部署前线上只读核验记录

日期：2026-05-09

## 目标

回答“V5.16 什么时候部署服务器”，并进入下一阶段：先只读确认 V5.16 是否已经在线、当前公网 asset、后端健康、微信相关路由状态，以及是否具备安全 SSH 条件。

本阶段不部署、不修改线上文件、不重启 PM2、不 reload Nginx。

## 模型建议

本阶段涉及服务器、PM2、Nginx、线上前端 root、ClawBot 与发布判断，因此模型建议为：

- GPT-5.5 high

## 执行前状态

- 最新 HEAD：`c3f2d7a feat: rename tutor to tree doctor`
- V5.16 已提交并 push 到 GitHub
- V5.16 本地构建通过，构建产物为 `dist/assets/index-D0g_6lWZ.js`
- V5.16 记录中明确写明：未部署、未连接服务器
- 当前工作区仍有大量历史脏改和未跟踪文件，不能清理、reset、revert

## 执行过程

### 公网首页核验

抓取 `https://shumai.cc/` 后确认：

- 当前公网首页 HTML 大小：2950 bytes
- 当前公网首页引用 asset：`/assets/index-BBjZDaHT.js`
- 线上 asset 中仍可检出旧称：
  - `我的学伴`
  - `树脉学长`
  - `问学长`

本地 V5.16 构建产物中已经是：

- `我的伙伴`
- `树博士`
- `问树博士数学问题`

因此，V5.16 前端命名改动还没有部署到公网。

### 公网 API 核验

- `GET https://shumai.cc/api/health`：正常，返回 `status: ok`
- `GET https://shumai.cc/api/wechat/status`：返回 401 `未登录`，说明微信状态路由仍在线
- `GET https://shumai.cc/api/study-plan`：返回 404，PagePlan 服务端路由仍未以该入口挂载上线

### SSH 只读核验

尝试使用非交互 SSH 只读登录：

- 用户：`ubuntu`
- 主机：`43.128.59.105`
- 方式：`BatchMode=yes`

结果：

- 登录失败：`Permission denied (publickey,password).`
- 未进入服务器
- 未读取 PM2
- 未读取 Nginx
- 未读取 DB
- 未确认 `shumai-bot` 是否存在或在线
- 未确认线上 `/opt/shumai/dist` 文件内容

## 部署判断

V5.16 不能算已部署。它目前处于：

- GitHub 已推送
- 本地构建已通过
- 公网仍未更新
- 需要服务器登录后执行前端静态发布

按 V4.97 之后的发布口径，下一次前端发布应先只读确认 Nginx 当前 root，当前已知默认发布目标仍按记录使用：

- `/opt/shumai/dist`

但实际执行前仍必须在服务器上再次读取 Nginx 配置确认。

## 建议部署时机

V5.16 可以作为下一次前端静态发布的内容上线，但前提是拿到安全服务器登录方式后执行：

1. 只读确认 `nginx -T` 当前 root
2. 备份当前 root
3. 在干净 release 或当前已验证构建中确认 `dist/assets/index-D0g_6lWZ.js`
4. 同步到 Nginx 当前 root
5. 验证公网首页 asset 已切到新文件
6. 验证公网页面不再出现 `我的学伴 / 问学长 / 树脉学长`
7. 验证 `/api/health` 正常

## 修改文件

- `tasks.md`
- `文档/树脉系统建设记录.md`
- `文档/V5.17-V5.16部署前线上只读核验记录.md`

## 边界

- 未部署
- 未进入服务器
- 未修改线上文件
- 未 reload Nginx
- 未重启 PM2
- 未保存 PM2
- 未执行 SQL
- 未修改数据库 schema
- 未触碰 `server/schema.sql`
- 未触碰 TTS / ASR / SVG / 微信 bot / 动画生成
- 未记录任何密码、token、API key 或数据库连接串

## 结论

V5.16 尚未部署。当前公网仍是旧前端 asset，学生仍会看到旧称。下一步需要安全服务器登录方式，才能进入 V5.18 前端静态发布。

## 下一步

建议下一阶段做 **V5.18 树博士前端静态发布**。

模型建议：

- GPT-5.5 high

开始条件：

- 提供安全 SSH 登录方式，或在用户本机终端完成服务器登录授权
- 只发布前端静态文件
- 不重启 PM2
- 不执行 SQL
- 不修改 Nginx 配置
