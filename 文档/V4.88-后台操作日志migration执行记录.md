# V4.88 后台操作日志 migration 执行记录

日期：2026-05-09

## 目标

基于 V4.87 已完成的 PostgreSQL 备份，执行后台操作日志 migration：

- `server/migrations/20260509_admin_operation_logs.sql`

本阶段只执行这一份 additive migration，并验证表、索引、约束和幂等性。

## 前置条件

V4.87 备份目录：

- `/home/ubuntu/shumai-backups/v4.87-db-precheck-20260509-180636`

V4.87 备份文件：

- `postgres-pre-v4.88.dump`

V4.87 备份校验：

- `postgres-pre-v4.88.dump: OK`

备份 SHA256：

- `5b66077890da4394b41a3290b5d1de3443f881bf0eba28e4212b3287fea812e3`

## migration 执行结果

执行记录目录：

- `/home/ubuntu/shumai-backups/v4.88-admin-operation-logs-migration-20260509-181249`

migration 文件：

- `/opt/shumai-releases/v4.85-20260509-175454/repo/server/migrations/20260509_admin_operation_logs.sql`

migration SHA256：

- `f44b2c9dceea93a82c246e866f9c539e0c480fbf756607c7b92e03a94ec62081`

执行前：

- `admin_operation_logs` 不存在

执行后：

- `admin_operation_logs` 已存在
- 记录数：`0`
- public 表数量：`12`
- 3001 health 正常

## 表结构验证

字段数量：

- `14`

字段清单：

- `id:bigint:NO`
- `admin_user_id:integer:YES`
- `admin_role:text:YES`
- `action:text:NO`
- `target_type:text:NO`
- `target_id:integer:YES`
- `target_key:text:YES`
- `target_name:text:YES`
- `before_value:jsonb:NO`
- `after_value:jsonb:NO`
- `reason:text:YES`
- `source:text:YES`
- `request_id:text:YES`
- `created_at:timestamp with time zone:NO`

索引数量：

- `6`

索引清单：

- `admin_operation_logs_pkey`
- `idx_admin_operation_logs_action_time`
- `idx_admin_operation_logs_admin_time`
- `idx_admin_operation_logs_created_at`
- `idx_admin_operation_logs_target`
- `idx_admin_operation_logs_target_key`

约束数量：

- `6`

约束清单：

- `admin_operation_logs_action_not_blank:c`
- `admin_operation_logs_admin_user_id_fkey:f`
- `admin_operation_logs_pkey:p`
- `admin_operation_logs_request_id_length:c`
- `admin_operation_logs_source_length:c`
- `admin_operation_logs_target_type_not_blank:c`

## 幂等性复核

本阶段执行了第二次 migration 复核。

结果：

- 二次执行成功
- 只返回 already exists / skipping notices
- `admin_operation_logs` 记录数仍为 `0`
- 索引数量仍为 `6`

说明当前 migration 的 `CREATE TABLE IF NOT EXISTS` 与 `CREATE INDEX IF NOT EXISTS` 行为符合预期。

## 清理动作

本阶段清理了临时脚本：

- `/home/ubuntu/v488_migration.sh`
- `/home/ubuntu/v488_idempotency.sh`

执行记录保留在：

- `/home/ubuntu/shumai-backups/v4.88-admin-operation-logs-migration-20260509-181249`

## 本阶段未执行

- 未修改 `/opt/shumai`
- 未修改 `/var/www/shumai`
- 未复制 `dist`
- 未 reload Nginx
- 未重启 PM2
- 未保存 PM2
- 未切换线上服务路径
- 未部署前端
- 未部署后端
- 未创建 `prompt_skills`
- 未执行其他 migration
- 未执行破坏性 SQL

## 结论

V4.88 后台操作日志 migration 执行成功。

当前数据库已经具备 `admin_operation_logs` 表，可支持后台操作日志 API 的持久化写入与读取。

但正式切换 release 后端前仍需处理：

- `prompt_skills` 当前线上库不存在
- GitHub 最新后台 Skill 管理依赖 `prompt_skills`
- 3001 / 3002 health name 仍存在 `树脉后端` 与 `数脉后端` 差异

## 下一步

建议下一阶段：

- **V4.89 prompt_skills schema 差异评估**：只做 schema 读取与 migration 方案，不直接建表。
- **V4.90 后台操作日志 API 旁路写读验证**：在 3002 临时进程上验证 `admin_operation_logs` API 是否能写入和读取。
- **V4.91 后台操作日志正式上线验证**：在 schema 风险处理后再切换后端与前端。
