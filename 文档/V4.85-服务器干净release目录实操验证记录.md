# V4.85 服务器干净 release 目录实操验证记录

日期：2026-05-09

## 目标

按 V4.84 方案，在服务器创建独立干净 release 目录，验证 GitHub 最新代码可以在不覆盖 `/opt/shumai`、不部署、不重启 PM2、不执行 SQL 的前提下完成安装和构建。

## 前置处理

V4.84 本地 commit 曾因本机到 GitHub HTTPS 不稳定而未及时推送。本阶段开始后先重试 GitHub，成功推送：

- commit：`7e034e3 docs: plan clean server release`
- 远端 `main`：`7e034e35bf640c0d5b9512ec5c0e984bd387e6bd`

随后进入服务器 release 验证。

## 服务器 release 验证结果

release 目录：

- `/opt/shumai-releases/v4.85-20260509-175454`

代码目录：

- `/opt/shumai-releases/v4.85-20260509-175454/repo`

Git 验证：

- clone 来源：`https://github.com/qingdaomic/shumai-2026.git`
- 分支：`main`
- HEAD：`7e034e35bf640c0d5b9512ec5c0e984bd387e6bd`
- 状态：`## main...origin/main`

前端验证：

- `npm ci`：通过
- `npm run build`：通过
- `dist/index.html`：存在
- `dist` 大小：`1.5M`
- 构建产物：
  - `dist/index.html`
  - `dist/assets/vendor-libs-DYLXRpC5.js`
  - `dist/assets/vendor-react-Bpq61jYu.js`
  - `dist/assets/index-BBjZDaHT.js`
  - `dist/assets/data-D-iFhHDm.js`

前端依赖审计提示：

- `npm ci` 后提示 2 个 moderate severity vulnerabilities
- 本阶段不执行 `npm audit fix`
- 后续可单独做依赖审计任务

后端验证：

- `cd server && npm ci --omit=dev`：通过
- 后端依赖审计：`found 0 vulnerabilities`
- `node --check index.js`：通过

后台操作日志文件确认：

- `server/migrations/20260509_admin_operation_logs.sql`：存在
- `server/services/admin-operation-logs.js`：存在
- `server/api/admin.js` 包含 `/operation-logs` GET / POST 路由
- `src/App.jsx` 包含后台操作日志读取与写入调用

## 清理动作

本阶段清理了前一次 GitHub 代推尝试留下的临时材料：

- `/home/ubuntu/shumai-v4.84.bundle`
- `/home/ubuntu/shumai-push-v4.84-*`

清理后只保留 V4.85 release 验证目录。

## 本阶段未执行

- 未修改 `/opt/shumai`
- 未修改 `/var/www/shumai`
- 未复制 `dist`
- 未 reload Nginx
- 未执行 SQL
- 未备份或修改 PostgreSQL
- 未启动 3001
- 未启动 3002
- 未重启 PM2
- 未保存 PM2
- 未切换线上服务路径
- 未部署前端
- 未部署后端

## 结论

V4.85 验证通过。

GitHub 最新 `main` 可以在独立 release 目录中完成：

- 干净 clone
- 前端依赖安装
- 前端生产构建
- 后端依赖安装
- 后端入口语法检查
- 后台操作日志关键文件确认

这说明下一阶段可以继续做 3002 旁路后端演练，但仍然不能直接切换线上服务，因为还需要确认生产环境变量注入、root PM2 管理方式、数据库备份和 migration 顺序。

## 下一步

建议下一阶段：

- **V4.86 线上切换前演练**：基于 `/opt/shumai-releases/v4.85-20260509-175454/repo/server` 生成临时 PM2 配置，用 3002 做旁路 health 验证，不抢 3001，不保存 PM2。
- **V4.87 数据库备份与 migration 验证**：在正式切换前备份 PostgreSQL，并执行 `server/migrations/20260509_admin_operation_logs.sql`。
- **V4.88 后台操作日志正式上线验证**：切换后端、前端静态文件、验证后台操作日志读写与页面显示。
