# V4.87 数据库备份与 migration 预检记录

日期：2026-05-09

## 目标

在执行后台操作日志 migration 前，先完成线上 PostgreSQL 备份与只读表结构预检。

本阶段只做：

- 确认数据库连接方式
- 备份当前线上数据库
- 检查 `admin_operation_logs` 是否已存在
- 保存 migration 文件副本和 SHA256
- 导出当前 public 表清单

本阶段不执行 migration，不改表，不部署，不重启服务。

## 连接来源

当前 root PM2 进程环境中只包含：

- `NODE_ENV`
- `PORT`

没有直接包含 `DATABASE_URL`。

数据库连接信息存在于服务器配置文件中。本阶段只在服务器命令内部读取连接值，没有输出连接串，也没有写入文档。

## 备份结果

备份目录：

- `/home/ubuntu/shumai-backups/v4.87-db-precheck-20260509-180636`

数据库：

- `shumai`

数据库用户：

- `shumai`

备份文件：

- `postgres-pre-v4.88.dump`

备份格式：

- `pg_dump -Fc`

备份大小：

- `172K`

备份 SHA256：

- `5b66077890da4394b41a3290b5d1de3443f881bf0eba28e4212b3287fea812e3`

## migration 文件校验

来源文件：

- `/opt/shumai-releases/v4.85-20260509-175454/repo/server/migrations/20260509_admin_operation_logs.sql`

备份目录内副本：

- `20260509_admin_operation_logs.sql`

migration SHA256：

- `f44b2c9dceea93a82c246e866f9c539e0c480fbf756607c7b92e03a94ec62081`

## 表结构预检

预检结果：

- `admin_operation_logs`：不存在
- `users`：存在
- `prompt_skills`：不存在
- `admin_operation_logs` 相关索引：无
- public 表数量：`11`

当前 public 表清单：

- `chat_history`
- `checkins`
- `class_students`
- `classes`
- `daily_tasks`
- `progress`
- `question_svgs`
- `system_config`
- `tts_usage`
- `users`
- `wrong_questions`

## 清理动作

本阶段清理了失败尝试中生成的空预检目录，只保留成功备份目录：

- `/home/ubuntu/shumai-backups/v4.87-db-precheck-20260509-180636`

同时删除临时脚本：

- `/home/ubuntu/v487_db_precheck.sh`
- `/home/ubuntu/v487_table_list.sh`

## 本阶段未执行

- 未执行 `server/migrations/20260509_admin_operation_logs.sql`
- 未创建 `admin_operation_logs`
- 未修改任何数据库表
- 未执行破坏性 SQL
- 未部署前端
- 未部署后端
- 未复制 `dist`
- 未 reload Nginx
- 未重启 PM2
- 未保存 PM2
- 未切换线上服务路径
- 未修改 `/opt/shumai`
- 未修改 `/var/www/shumai`

## 结论

V4.87 备份与 migration 预检通过，可以进入 migration 执行阶段。

但有一个正式切换前必须处理的风险：

- GitHub 最新 release 中后台 Skill 管理依赖 `prompt_skills`
- 线上数据库当前没有 `prompt_skills`
- 即使执行 `admin_operation_logs` migration，后台 Skill 管理正式切换仍可能因为 `prompt_skills` 缺表而不可用

因此下一阶段可以只执行 `admin_operation_logs` 这份 additive migration，但正式切换 release 后端前，必须单独评估 `prompt_skills` 相关 schema。

## 下一步

建议下一阶段：

- **V4.88 后台操作日志 migration 执行**：基于本次备份，执行 `20260509_admin_operation_logs.sql`，然后验证表、索引和幂等性。
- **V4.89 prompt_skills schema 差异评估**：确认后台 Skill 管理所需表是否需要补 migration。
- **V4.90 后台操作日志正式上线验证**：在 schema 风险处理后，再切换后端和前端。
