# V5.13 SKE 提问斜杠权重回流记录

日期：2026-05-09

## 目标

把提问斜杠的推荐顺序从“只看静态权重和题目上下文”，升级为“结合近 90 天历史点击、AI 使用和反馈回流”的动态排序。

## 模型建议

本阶段属于本地代码与文档校准，建议：

- 纯文档 / 本地审查：GPT-5.4-Mini
- 服务器 / 部署 / PM2 / Nginx / 数据库：GPT-5.5 high

## 执行前状态

- `git status -sb` 显示工作区仍有大量历史脏改
- 最新提交：`74bf3c5 docs: record v5.12 page plan summary polish`
- 当前任务入口仍指向 SKE-4.1
- 本阶段未连接服务器

## 执行过程

- 在后端 `prompt-skills` 推荐逻辑中加入事件统计回流
- 让 `/api/skills/recommend` 读取近 90 天 `prompt_skill_events`
- 将历史反馈折算进推荐分，并把分数带回前端
- 前端斜杠菜单保留这个分数，避免本地二次排序把后端效果洗掉
- 增加本地验证脚本，确认历史反馈更好的 skill 会排到前面

## 验证结果

- `node --check server/services/prompt-skills.js` 通过
- `node --check server/api/skills.js` 通过
- 假数据推荐验证中，历史反馈更好的 skill 被顶到第一
- `npm run build` 通过
- `git diff --check` 通过

## 修改文件

- `server/services/prompt-skills.js`
- `server/api/skills.js`
- `src/App.jsx`
- `server/scripts/verify-skills-api.js`
- `tasks.md`
- `文档/树脉系统建设记录.md`
- `文档/V5.13-SKE提问斜杠权重回流记录.md`

## 边界

- 未部署
- 未 reload Nginx
- 未重启 PM2
- 未保存 PM2
- 未执行 SQL
- 未修改数据库 schema
- 未触碰 `server/schema.sql`
- 未触碰 `server/api/study-plan.js`
- 未触碰 TTS、ASR、SVG、微信、动画相关线上能力
- 未记录任何密码、token、API key 或数据库连接串

## 结论

SKE-4.1 已完成，提问斜杠开始具备历史反馈回流能力。

## 下一步

- 继续做 SKE-4.2.1，把个体偏好和题型更细地折算进推荐分
- 或回到本地页面体验，继续打磨 PagePlan 的视觉层
