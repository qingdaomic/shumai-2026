# V4.93 后端 health 命名校准与 3002 复验记录

日期：2026-05-09

## 目标

处理 V4.92 发现的上线前命名差异：

- 当前线上旧后端 health name：`树脉后端`
- GitHub release 后端 health name：`数脉后端`

本阶段将 release 后端 health 与启动日志统一为 `树脉后端`，然后用 3002 做快速旁路复验。

本阶段不切换正式 3001，不发布前端，不 reload Nginx，不保存 PM2。

## 模型建议

本阶段涉及小代码改动、GitHub 推送、服务器 release、临时 PM2 与 3002 API 验证，建议使用 GPT-5.5 high。

## 本地代码处理

本地 `server/index.js` 工作树存在历史脏改：

- TTS / ASR
- study-plan
- animation cron

因此本阶段没有直接 `git add server/index.js` 全文件，而是只把 HEAD 中两处命名改动放入暂存区：

- `/api/health` 返回的 name：`数脉后端` -> `树脉后端`
- 启动日志：`数脉后端` -> `树脉后端`

代码提交：

- `bd146411642f56de23874f4465f293f481ef2bd9`
- commit message：`fix: align backend health name`

本地校验：

- `git diff --cached --check`：通过
- staged `server/index.js`：`node --check` 通过

## 服务器 release

新建干净 release：

- `/opt/shumai-releases/v4.93-20260509-185646/repo`

release HEAD：

- `bd146411642f56de23874f4465f293f481ef2bd9`

release 状态：

- `git status --short`：0
- `server/index.js` 中 `树脉后端` 命中：1
- `server/index.js` 中 `数脉后端` 命中：0

服务端准备：

- `npm ci --omit=dev`：通过
- `node --check index.js`：通过

## 3002 快速复验

启动方式：

- PM2 进程名：`shumai-api-v4.93`
- 端口：3002
- `DOTENV_CONFIG_PATH=/opt/shumai/server/.env`
- `--no-autorestart`
- 不保存 PM2

启动前 PM2：

- `shumai-api:online`

启动后 PM2：

- `shumai-api:online`
- `shumai-api-v4.93:online`

health 验证：

- 3002：`{"status":"ok","name":"树脉后端","version":"1.0.0"}`
- 3001：`{"status":"ok","name":"树脉后端","version":"1.0.0"}`

后台 API 验证：

- 服务器内生成短期管理员 token
- 不输出 token
- `GET /api/admin/skills?limit=20`：通过
  - items：20
  - summary total：41
- `POST /api/skills/recommend`：通过
  - source：`db`
  - returned：2

数据库计数：

- `prompt_skills`：41
- `prompt_skill_events`：0
- `admin_operation_logs`：0

## 收尾状态

已停止并删除临时 PM2：

- 删除后 PM2：`shumai-api:online`
- 3002：连接拒绝，符合已停止预期
- 3001：健康检查正常，name 为 `树脉后端`

## 本阶段边界

已执行：

- 最小后端命名校准
- GitHub push
- 新建 V4.93 干净 release
- server 生产依赖安装
- server 语法检查
- 临时 3002 启动
- 3002 health 验证
- 后台 Skill 管理 API 快速验证
- SKE 推荐 API 快速验证
- 停止并删除临时 3002 PM2

未执行：

- 未切换正式 3001
- 未部署前端
- 未复制 `dist`
- 未 reload Nginx
- 未重启正式 PM2
- 未保存 PM2
- 未修改 `/opt/shumai`
- 未修改 `/var/www/shumai`
- 未执行 SQL
- 未触碰 study-plan / TTS / ASR / SVG / 微信 / 动画相关线上能力

## 结论

V4.93 通过。

正式切换前的 health 命名差异已经消除，3002 快速复验确认：

- release 后端可启动
- health name 已统一为 `树脉后端`
- 后台 Skill 管理 API 可访问
- SKE 推荐使用数据库来源
- 测试未产生脏数据
- 临时 PM2 已清理

## 下一步

建议下一阶段：

- **V4.94 后台操作日志正式上线执行**：按 V4.92 操作卡执行正式 release、切换 3001、验证 API、发布前端，并保留回滚路径。
