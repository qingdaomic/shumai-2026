# V4.80 SKE/PET 后台操作历史类型筛选记录

日期：2026-05-09

## 目标

V4.79 已经把操作历史详情转成中文摘要。V4.80 继续给历史面板加一个轻量筛选，让管理员只看降权或只看改写。

## 本阶段完成

- Skill 操作历史面板新增类型筛选
- 支持：
  - 全部
  - 降权
  - 改写
- 显示当前筛选数量 / 总数量
- 筛选只作用于当前已加载记录
- 不新增后端接口
- 不改变数据库查询逻辑

## 截图记录

- `文档/截图记录/2026-05-09-v4-80-operation-history-filter-admin.png`

## 边界

- 不执行 SQL
- 不连接本地或线上数据库
- 不修改 `server/schema.sql`
- 不新增后端接口
- 不部署
- 不服务器操作
- 不 SSH
- 不 PM2 / Nginx / PostgreSQL
- 不触碰 TTS / ASR / study-plan / deploy / public animations / dist-singlefile / local-notes / `.env*`

## 下一步

- 后续可做后台操作日志真实上线验证任务：执行 migration、部署后端、实测读写
- 服务器 / 数据库操作必须单独授权
